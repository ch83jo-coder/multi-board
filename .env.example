NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# 公開URL。canonical、OG、robots、sitemap、JSON-LDで使用します。
NEXT_PUBLIC_SITE_URL=https://panmoa.com
# Server-only. Required only for admin board management actions.
SUPABASE_SERVICE_ROLE_KEY=
# Server-only. Required for the manual seed and content bot scripts.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional. Used only with content-bot --with-images.
OPENAI_IMAGE_MODEL=gpt-image-2
