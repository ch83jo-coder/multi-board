{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | while read -r f; do case \"$f\" in *.ts|*.tsx|*.mjs|*.css) [ -f \"$f\" ] && ./node_modules/.bin/biome format --write \"$f\" >/dev/null 2>&1;; esac; done; exit 0"
          }
        ]
      }
    ]
  }
}
